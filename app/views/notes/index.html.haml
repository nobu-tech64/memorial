.header
  .header__title
    まもののむれ日記

.body
  .sidebar
    .sidebar__text
      = link_to '日記を書く', new_note_path
    .sidebar__calendar
      = month_calendar events: @notes do |date, notes|
        = date.day
        - notes.each do |note|
          = note.name
  .main

